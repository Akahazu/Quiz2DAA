[project]
name = "DAA-finals"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "perfplot",
    "numpy",
    "pygame",
    "pytmx",
]

[dependency-groups]
dev = [
    "ruff>=0.15.17",
]

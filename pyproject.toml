[project]
name = "DAA-finals"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "perfplot",
    "numpy",
    "pygame",
    "pytmx",
    "pandas>=3.0.3",
    "matplotlib>=3.11.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.17",
]
